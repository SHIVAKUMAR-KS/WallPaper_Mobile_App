import { Text,View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function account(){


    return <SafeAreaView style={{backgroundColor:"yellow"}}>
        <Text>Account page</Text>
        </SafeAreaView>
}